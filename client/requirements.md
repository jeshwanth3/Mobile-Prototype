## Packages
framer-motion | Page transitions and complex animations for workout tracking
recharts | Visualizing workout progress and volume stats
date-fns | Formatting dates for workout logs and schedules
clsx | Conditional class merging
tailwind-merge | Merging tailwind classes effectively

## Notes
- Mobile-first design approach is critical
- Dark mode is default
- Auth integration assumes /api/auth/* endpoints exist
- Icons use lucide-react (already in base stack)
